**difficulty level:** [easy] [**medium**] [hard] [very hard]

```cpp
#include <iostream>

int main()
{
	int x{ 1 };

	std::cout << [x = x++](int y) {return x * y; }(x++);
	std::cout << [x = ++x](int y) {return x * y; }(++x);
}
```
